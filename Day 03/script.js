//Write a function calculates perimeter of Rectangle.

function perimeterOfRectangle(length, width) {
    return 2 * (length + width);
  }

  document.getElementById("one").innerHTML = perimeterOfRectangle(5, 3);
  
